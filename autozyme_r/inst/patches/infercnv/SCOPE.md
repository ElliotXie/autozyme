# autozyme `infercnv` (R) — supported parameter scope

This patch accelerates the function(s) below but is **validated only for a
specific parameter envelope**. Within that envelope the fast path reproduces the
upstream result (to the stated output-equivalence class); outside it, behavior
is one of: falls back to upstream, raises, or — for a few documented
approximations — silently approximates.

Supported parameters (e.g. `n_comps` / `npcs`, resolution, the data itself) can
be set freely. Only the parameters listed under each entry change behavior.

_Auto-generated from `scripts/patch_scope.tsv`. Do not edit by hand — run
`scripts/gen_scope_docs.py`._


## `infercnv::run (HMM=TRUE)`

- **In-scope output equivalence:** bit_exact
- **Validated at:** `set.seed(1234); infercnv::run(<data: CreateInfercnvObject from melanoma_tirosh/gbm_neftel/hnscc/brca Seurat counts>, cutoff = 1, out_dir = <tempdir>, cluster_by_groups = TRUE, analysis_mode = "samples", denoise = FALSE, HMM = TRUE, num_threads = 1, save_rds = FALSE, save_final_rds = FALSE, no_plot = TRUE, no_prelim_plot = TRUE, plot_steps = FALSE) — HMM_type and BayesMaxPNormal left at upstream defaults "i6" and 0.5`
- **Supported scope:** Correctly accelerates infercnv::run with HMM=TRUE, analysis_mode="samples", HMM_type="i6" (default), single-threaded (num_threads=1), window_length=101, dense or coercible-to-dense expression with NO NAs, and tumor-subcluster partition_method="none" (what analysis_mode="samples" implies). Under HMM=TRUE the streamlined fast_run body is deliberately NOT used (its guard requires isFALSE(args$HMM)); instead a gc-suppressed clone of upstream run() executes with 19 monkey-patched internals (smooth_window/by_chromosome, normalize, log2xplus1/invert_log2, subtract_ref+threshold fusion, center, Viterbi, state-consensus, cell_prob/cnv_prob, hspike trend, define_cnv_gene_regions, run_gibb_sampling, inferCNVBayesNet saveRDS-shim, define_signif_tumor_subclusters). Verified bit-exact (max_abs_diff=0, pearson=1, hmm_state_agreement=1) across 5 datasets/tiers on macOS+Windows AT FIXED set.seed(1234) ONLY. Most matrix kernels are genuine mathematical equivalents; the HMM Gibbs step is an approximation that happened to reproduce identical discrete state calls on the benchmarked data.
- **Out-of-scope behavior:** ⚠ **Documented approximation.** Correct for the validated configuration below; results may differ outside it and there is no automatic fall-back, so stay within the stated scope (or deactivate the patch).
- **Approximation details:** DANGEROUS UNGUARDED MCMC TRUNCATION: fast_run_gibb_sampling (patch.R:612-641) silently and unconditionally replaces upstream's Gibbs settings. Upstream i6 uses 6 chains (inits epsilon=1..6), a stats::update(model,200) burn-in, and n.iter=1000. The patch uses only 3 chains (inits 1,3,6 for i6), NO burn-in update step, and n.iter=20. There is NO guard checking this approximation is valid; bit-exact HMM state calls were only verified on the 5 benchmarked datasets. On data with more ambiguous CNV posteriors the truncated/under-mixed MCMC can yield different theta/epsilon estimates, a different BayesMaxPNormal=0.5 posterior-probability filter outcome, and thus different final HMM calls — silently, with no error. (final_audit.md section 1.10 incorrectly states n.chains=3 and n.adapt=500 'same as upstream' — they are not for i6.) ; HMM_type: i3 vs i6 handled in cell_prob (3 vs 6 states) and gibb inits, but .get_state_emission_params hard-codes the 6 i6 cnv levels (cnv:0.01/0.5/1/1.5/2/3) — matches upstream which is also i6-only there, so i3 emission would defer to upstream's same structure; i3 path is far less exercised (benchmark used i6 only). ; Single-threaded only: num_threads>1 is not honored by the patched fast path; task.yaml declares threading: not_applicable. ; analysis_mode != 'samples' (e.g. 'subclusters','cells'): runs through gc-skip clone of upstream run with patched sub-functions — accelerated but the streamlined fast_run is never engaged; partition_method != 'none' makes define_signif_tumor_subclusters fall back to upstream. ; NA in expression matrix: fast_smooth_window falls back to upstream .smooth_window per-chromosome (slow, but correct). ; Sparse input matrices are forcibly densified (as.matrix) in many kernels — potential OOM on very large sparse data. ; Fused threshold-clamp state (.zyme_fused_state) couples subtract_ref_expr_from_obs to apply_max_threshold_bounds via a shared env; relies on upstream's exact call order. If users call these internals independently or upstream reorders, threshold is double-applied or skipped silently. ; saveRDS shim keyed on basename=='MCMC_inferCNV_obj.rds'; if upstream renames the file the I/O skip silently degrades (still correct, just slower). ; Tested only against infercnv 1.24.0; 19 non-exported internals patched — any upstream refactor can break or silently bypass the fast path.

