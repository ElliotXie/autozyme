# Method family — <fill in: package or workflow name>

**Verdict:** TBD — fill during init Step 1.

<one short paragraph explaining the call chain and the verdict — for "single", note the public API surface is one function and call out that internal helpers are private; for "split", list each recommended sibling with role, estimated cost share, and the reason it qualifies>

## Suggested next CLI invocations  (only if siblings recommended)

```
cd <new_task_dir> && python <path>/bin/zyme init <upstream_url> <function> --dataset <path>
...
```
