"""evaluate.py — stub for the synthetic fixture.

`zyme profile` skips evaluate (skip_evaluate=True), so this is only invoked
if someone runs `zyme run` against the fixture. It always passes — we
don't care about output correctness, only profiling behavior.
"""
print("synthetic_evaluate: ok")
