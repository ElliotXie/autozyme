id: iterate_core
stage: iterate
prompt_slot: iterate
field: Bio
description: Core benchmark suite for Bio iterate-prompt changes. Three tasks chosen to probe distinct prompt failure modes.
default_reps: 2
default_budget_rounds: 50

tasks:
  - id: findallmarker
    template: findallmarker
    probe: aggressiveness
  - id: mast
    template: mast
    probe: concordance_discipline
  - id: tradeseq_fitgam
    template: tradeseq_fitgam
    probe: native_reach
  - id: scvelo_recover_dynamics
    template: scvelo_recover_dynamics
    probe: native_reach
